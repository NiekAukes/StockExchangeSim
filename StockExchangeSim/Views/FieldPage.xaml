<UserControl
    x:Class="StockExchangeSim.Views.FieldPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StockExchangeSim.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300" Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Row="0" Margin="20">
        <SymbolIcon x:Name="symbolIcon" Symbol="AllApps"/>
        </Viewbox>
        <Viewbox Grid.Row="1" Margin="0,5,0,5">
            <TextBlock x:Name="FieldTextBlock"  Text="prevField1" FontFamily="Nunito" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Viewbox>

    </Grid>
</UserControl>
